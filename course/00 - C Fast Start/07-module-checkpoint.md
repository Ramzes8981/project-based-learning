# 0.7 — Checkpoint: можешь ли ты объяснить программу только изученными механизмами

**Время:** ~60–90 мин  
**С телефона:** вопросы — да; project check — ПК

← [`06-make-build-test.md`](06-make-build-test.md) · ↑ [`README`](README.md)

## Explain

Без конспекта объясни:

1. Почему `main.c` не является исполняемым файлом?
2. Что compiler делает на нужном нам сейчас уровне модели?
3. Почему integer division отличается от floating-point division?
4. Почему массив из `N` элементов имеет допустимые индексы `0..N-1`?
5. Чем число используемых элементов отличается от размера фиксированного массива?
6. Почему C string требует конечный `\0`?
7. Как `struct` помогает не рассинхронизировать связанные поля?
8. Какая проблема появляется, когда implementation разбивается на несколько `.c`, и зачем после этого нужен linker?
9. Почему header change должен участвовать в build dependencies?

## Build

MiniKV проходит [`project/ACCEPTANCE.md`](project/ACCEPTANCE.md) и [`project/TESTS.md`](project/TESTS.md).

## Debug story

Запиши один реальный случай:

```text
симптом
→ гипотеза
→ проверка
→ причина
→ исправление
→ тест, который теперь ловит регрессию
```

## Transfer

Добавь **одну** новую операцию поведения без механизмов из следующего модуля. Например `COUNT` или вывод всех занятых записей.

## Gate

Ты готов идти дальше, если можешь полностью объяснить MiniKV через уже пройденные values/functions/arrays/C strings/structs/multifile build и не вынужден ссылаться на ещё не изученную модель памяти.
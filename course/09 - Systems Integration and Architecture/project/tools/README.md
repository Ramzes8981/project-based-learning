# Capstone tools

Infrastructure only; server code remains student-owned.

- Reuse/adapt Module 5 `client.py`/`loadgen.py` if wire protocol stays compatible.
- `failure_injection.py` mutates **copies** of storage files for corruption/recovery tests.
- Load test report must record workload/environment; a tool output without context is not architecture evidence.
